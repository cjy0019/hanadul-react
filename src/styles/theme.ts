type Colors = {
  lightGreen: string;
  mustard: string;
  lightRed: string;
};

const colors = {
  lightGreen: '#2799A3',
  mustard: '#FFD84D',
  lightRed: '#FF7E7E',
  white: '#fff',
};

const theme = {
  colors,
};

export default theme;
